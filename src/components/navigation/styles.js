const styles = () => {
  return ({
    navigation: {
      display: 'flex',
      justifyContent: 'space-between',
      padding: 10,
      backgroundColor: 'lightblue'
    }
  });
};

export default styles;